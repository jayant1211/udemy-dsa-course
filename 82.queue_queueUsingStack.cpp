/**
 * using 2 stacks we can implement a queue
 * enqueue - in 1st stack
 * 
 * dequeue - 
 * check if 2nd stack is empty
 *  if yes, transer elements from 1st stack till 1st stack is empty
 *  if no, then pop out
 *
 * enqueue -
 * in first stack only
*/